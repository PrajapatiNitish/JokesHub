# JokesHub
Start Making my second website - JokesHub

# Installed Dependencies
1. cd Frontend
2. Install `npm install -D sass-embedded` for sass file
3. Install `npm run dev`
4. Install `npm install` or `npm i`

5. cd ..
6. cd Backend
7. Install `npm install` or `npm i`